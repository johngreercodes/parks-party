import React from 'react'

const ParkItem = (props) => {
    return (
        <div>
            <h1>hi</h1>
        </div>
    )
}

export default ParkItem

// props.parks.map((park)=>(
//     <div key={park.id}>
//         <h1>sup</h1>
//     </div>
// ))